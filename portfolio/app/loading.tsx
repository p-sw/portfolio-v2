import "@/app/loading.css";

export default function Loading() {
  return <div>
    <div className={`center`}>
      <p>Loading<span className={`d a`}>.</span><span className={`d b`}>.</span><span className={`d c`}>.</span></p>
    </div>
  </div>
}